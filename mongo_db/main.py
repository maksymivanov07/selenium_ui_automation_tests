from mongo_db.mongo_collection import MongoClient

if __name__ == '__main__':
    mongo_docs = MongoClient()
    # mongo_docs.find_all()
    # mongo_docs.find_one()
    # mongo_docs.insert_one({'principle': 'Max I'})
    # mongo_docs.insert_many([{'principle': 'Theodor R'}, {'principle': 'Abraham L'}])
    # mongo_docs.find_chosen_one({'principle': 'Max I'})
    # mongo_docs.delete({'principle': 'Max I'})
    # mongo_docs.update({'principle': 'Max I'}, {'principle': 'Josef B'})
    # mongo_docs.sort_by('capacity')